describe.skip("actions", () => {
  it("should create an action to fetch launches", () => {
    /* fill in test */
  });

  // Tests:
  // GET /launches should return data with properties: rocket, name, details
  // GET /rockets should return data with properties: id, name
});
